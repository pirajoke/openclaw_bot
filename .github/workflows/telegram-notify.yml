name: Telegram Notify

on:
  issues:
    types: [opened, closed, reopened]
  pull_request:
    types: [opened, closed, ready_for_review]

jobs:
  notify:
    uses: pirajoke/openclaw_bot/.github/workflows/notify-telegram.yml@main
    with:
      topic_id: "4"
    secrets: inherit
